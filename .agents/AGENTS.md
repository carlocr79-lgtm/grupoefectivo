# UI Design Guidelines

- **Tab Buttons (Main vs Secondary)**: Main tab buttons (like `.btn-main-tab`) must always be visually distinguished from secondary tabs to maintain UI hierarchy. When a main tab is active, it should use a solid dark blue background (`var(--brand-secondary)`) with white text and icons. It should NOT use the light blue background or simple outline styles that are reserved for secondary/sub-tabs.
